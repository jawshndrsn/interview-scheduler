<h1>Interview Panels</h1>

<p>
Interview panels coordinate a schedule of sessions through which potential candidates go through.
</p>

<table>
  <tr>
    <th>Candidate</th>
    <th>Email</th>
    <th>Position</th>
    <th>Date</th>
    <th>State</th>
    <th></th>
    <!-- <th></th>
    <th></th> -->
  </tr>

<% @panels.each do |panel| %>
  <tr>
    <td><%= panel.candidate %></td>
    <td><%= panel.email %></td>
    <td><%= panel.position %></td>
    <td><%= panel.date %></td>
    <td><%= panel_state(panel) %></td>
    <td><%= link_to 'Show', panel %></td>
    <!-- <td><%= link_to 'Edit', edit_panel_path(panel) %></td>
    <td><%= link_to 'Destroy', panel, :confirm => 'Are you sure?', :method => :delete %></td> -->
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Panel', new_panel_path %>
